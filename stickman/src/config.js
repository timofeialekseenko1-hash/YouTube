export const WIDTH = 1920;
export const HEIGHT = 1080;
export const FPS = 30;

// Overall character size. 1.0 = raw rig; larger fills more of the frame,
// like the reference. Feet stay anchored to the floor regardless.
export const FIGURE_SCALE = 1.5;
// Approx pelvis->foot distance in raw rig units (for floor anchoring).
export const FOOT_DROP = 156;
